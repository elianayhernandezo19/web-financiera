#!/bin/sh
# docker-entrypoint removed — Docker is no longer used in this project.
# If you need runtime env substitution for static builds, implement a
# small script in your deployment pipeline that writes `public/env.js` with
# the desired values before serving.
exit 0
